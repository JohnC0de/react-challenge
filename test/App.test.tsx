// import { expect, it, describe } from "vitest"
// import { render, screen, userEvent, waitForElementToBeRemoved } from "./test-utils"
// import App from "../Canvas"

// //App Challenge: Display a circle at the position of the click of the mouse on the canvas and three buttons on the page. One button should undo the last action, one should redo the last action and the third should clear the canvas.

// describe("App", () => {
//   it("Should click on four random spots on screen and return their position on console", async () => {
//     render(<App />)
//     const user = userEvent.setup()
//     const canvas = screen.getByRole("canvas")
//     const undoButton = screen.getByRole("button", { name: /undo/i })
//   })
// })
